{
  "name": "related-tabs-graph",
  "version": "1.0.0",
  "description": "Firefox extension for tracking related tabs",
  "license": "ISC",
  "author": "giladh",
  "type": "commonjs",
  "scripts": {
    "start": "web-ext run --source-dir src/",
    "build": "web-ext build --source-dir src/",
    "lint": "web-ext lint --source-dir src/"
  },
  "devDependencies": {
    "web-ext": "^7.12.0"
  }
}
